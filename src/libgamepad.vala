// Just a blank file so that meson names the generated vapi and header file
// 'libgamepad'. Manually naming them so using vala_args did do that but didnot
// work with declare_dependency, so this simple hack
